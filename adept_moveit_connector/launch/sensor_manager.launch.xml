<launch>

  <!-- This file makes it easy to include the settings for sensor managers -->  
  <!-- Get the kinect piped into the octomap -->
  <rosparam command="load" file="$(find adept_moveit_connector)/config/sensors_depth.yaml" />
  
  <!-- Params for the octomap monitor -->
  <param name="octomap_frame" type="string" value="adept_combined" />
  <param name="octomap_resolution" type="double" value="0.015" />
  <param name="max_range" type="double" value="1.5" />

  <!-- Load the robot specific sensor manager; this sets the moveit_sensor_manager ROS parameter -->
  <arg name="moveit_sensor_manager" default="ADEPT_VIPER_S650" />
  <include file="$(find adept_moveit_connector)/launch/$(arg moveit_sensor_manager)_moveit_sensor_manager.launch.xml" />
  
</launch>
